# Dependencies / build
node_modules/

# PRIVACY: real Tzivos Hashem soldier data — NEVER commit these.
# The tracked src/data/roster.demo.js is the ANONYMIZED demo (fake soldiers);
# the roster guard in vite.config.js refuses to build any roster whose first
# line lacks the "ANONYMIZED DEMO DATA" marker. A REAL roster regenerated by
# the converter lands at the ignored src/data/roster.generated.js — never
# import it from src/ and never `git add -f` it.
*soldiers*.html
*soldiers*.zip
/resources/
src/data/roster.generated.js

# local/editor
.DS_Store
*.local
